rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // COMPLETELY RELAXED RULES FOR LOCAL DEVELOPMENT
    // This allows the seeding script to work without permission issues
    match /{document=**} {
      allow read, write: if true;  // Allow everything in development
    }
  }
}
