# Several deps haven't published React 19 / ESLint 10 peer ranges yet (e.g.
# eslint-plugin-import, @vercel/analytics optional framework peers). We install
# with legacy peer resolution; this makes `npm install` AND Vercel's `npm ci`
# use the same mode so the lockfile stays in sync and CI installs don't fail.
legacy-peer-deps=true
